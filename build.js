/*
1. Display loading indicator
2. Concatenate dependencies (no requirejs): reqwest, bean, jQuery (for now), etc.
3. Write a simple loader for web mapping API, delaying any more loads until it is ready
4. Call NPMap.events.preinit (where???)
5. Build a concatenated file with all of the NPMap modules
6. Write a simple loader that loads all of the NPMap modules and concatenate it
*/